{
  "buildCommand": "cd 'Cross-platform ride-sharing app' && npm install && npm run build",
  "outputDirectory": "Cross-platform ride-sharing app/dist",
  "env": {
    "VITE_API_BASE_URL": "https://wasalni.onrender.com"
  }
}
